All project delivery dashboard scripts should be in this folder. It is possible to have one script file only and use a jquery function to execute specific code ie.
```
if($("#divid").length) {
  //code here
}

```
